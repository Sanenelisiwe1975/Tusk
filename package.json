{
  "name": "tusk",
  "version": "0.1.0",
  "private": true,
  "description": "Tusk: decentralized agent memory on Walrus + Sui",
  "scripts": {
    "demo": "pnpm --filter demo run demo",
    "demo:fallback": "pnpm --filter demo run fallback"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
